name: budget_wallet
description: تطبيق المحفظة الذكية - تتبع المصاريف وإدارة الميزانية بنظام الظروف الرقمية وتحديات التوفير، بدون إنترنت بالكامل
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.1.0
  shared_preferences: ^2.2.3
  fl_chart: ^0.68.0
  connectivity_plus: ^6.0.5
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  flutter_timezone: ^1.1.0
  path_provider: ^2.1.4
  share_plus: ^9.0.0
  file_picker: ^8.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
